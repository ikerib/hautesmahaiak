<form method="post" action="{{ path('app_herritarra_delete', {'id': herritarra.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ herritarra.id) }}">
    <button class="btn">Delete</button>
</form>
